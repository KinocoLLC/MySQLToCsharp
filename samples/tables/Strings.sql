CREATE TABLE `Strings` (
  `Id` INT NOT NULL,
  `S` VARCHAR(50) NOT NULL,
  `NS` VARCHAR(50) NULL,
  `T` TEXT NOT NULL,
  `NT` TEXT NULL,
  `MT` MEDIUMTEXT NOT NULL,
  `NMT` MEDIUMTEXT NULL,
  `LT` LONGTEXT NOT NULL,
  `NLT` LONGTEXT NULL,
  PRIMARY KEY (`Id`)
);
